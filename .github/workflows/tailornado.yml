name: CI auth

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  CI:
    uses: ShopOnGO/ShopOnGO/.github/workflows/template.yml@main
    with:
      service_name: auth-service
      dockerfile: ./Dockerfile
      compose_file: docker-compose.ci.yml
    secrets: inherit
